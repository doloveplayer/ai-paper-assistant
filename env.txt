conda create -n ai_agent python=3.10 -y
conda activate ai_agent

# 0. 自行下载vllm依赖的torch 否则会自动拉取最新版
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# 1. 核心大模型交互库 (哪怕你用本地模型，OpenAI SDK也是目前的行业标准接口)
pip install openai langchain langgraph

# 2. RAG 核心组件库 (LlamaIndex生态)
pip install llama-index 
pip install llama-index-embeddings-huggingface  # 本地运行 Embedding 必须
pip install llama-index-vector-stores-qdrant    # 本地轻量级向量数据库

# 3. 文档解析与 UI
pip install PyMuPDF streamlit pydantic

# 4. 模型下载与加速工具 (国内推荐使用 ModelScope 魔搭社区)
pip install modelscope transformers torch

pip install arxiv
pip install llama-index-readers-papers

pip install vllm