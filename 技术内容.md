# VRAG — 视觉增强学术研究智能体 技术深度分析

## 一、项目定位与解决的问题

### 1.1 核心痛点

传统的学术论文检索与分析工具存在以下关键局限：

| 痛点 | 传统方案局限 | VRAG 的应对 |
|------|-------------|------------|
| **图表/公式不可检索** | 文本 RAG 仅索引文字，论文核心信息（架构图、消融实验表、公式）完全丢失 | ColPali 视觉编码 + VLM 图文分析 + **ROI 智能裁剪**，精准定位核心区域 |
| **检索后无法深度研读** | 搜索工具只返回标题摘要，用户需手动下载阅读 | 自动下载→双路入库→多模态检索→VLM 生成分析报告，全自动闭环 |
| **单模态检索偏差** | 纯文本向量检索对"看图说话"型论文（CV 领域）召回率低 | 视觉+文本双路检索 + RRF 倒数排名融合，互补召回 |
| **视觉 Token 爆炸** | 全页论文图片送入 VLM，每张图约 2000 vision tokens，大量无效背景 | **ColPali 热力图 ROI 裁剪 + 25% 上下文边距 + 拼图合成**，节省 ~60% 视觉 Token |
| **长对话记忆退化** | 传统摘要式压缩导致记忆越来越模糊，无法保留关键细节 | **四层分层记忆 + Qdrant 外部化长期记忆**，结构化维护画像/上下文/备忘 |
| **重复 Prompt 开销** | 每次请求重发完整 System Prompt，浪费推理时间 | **vLLM Prefix Caching** + 静态→半静态→动态 Prompt 倒序拼接，≥60% 缓存命中 |
| **单卡 GPU 显存极度紧张** | ColPali (~4GB) + vLLM 文本 + vLLM 视觉共用一张 GPU | 懒加载模式 + 8-bit 量化 + 逐页渲染 + CPU 运行 BGE-M3 |

### 1.2 适用场景

- **CV 研究者**：快速检索恶劣天气目标检测等细分领域的最新论文，并直接询问"这篇论文的 neck 结构是什么？mAP 提升了多少？"
- **文献综述**：批量入库论文后，跨论文对比方法论、数据集、实验结果
- **论文复现**：精确定位论文中的网络架构图、超参数表、损失函数公式

---

## 二、技术架构全景

### 2.1 总体架构图

```
┌─────────────────────────────────────────────────────────┐
│                     用户交互层                            │
│  ┌──────────────┐  ┌──────────────────────────────────┐ │
│  │  CLI 终端     │  │  Chainlit Web UI (:8053)          │ │
│  │  agent_core   │  │  app_ui.py                       │ │
│  └──────┬───────┘  └──────────────┬───────────────────┘ │
└─────────┼──────────────────────────┼─────────────────────┘
          │                          │
          ▼                          ▼
┌─────────────────────────────────────────────────────────┐
│              Agent 核心：LangGraph 状态机                  │
│                                                          │
│  START → init_core → retrieve_memory → agent ⇄ tools    │
│                               ↓ (最终回答)                │
│                          cleanup_ephemeral               │
│                               ↓                          │
│                          update_profile → END            │
│                          extract_memory (后台线程)        │
│                                                          │
│  分层记忆: Core SOP | Profile JSON | Working Context     │
│           Ephemeral Memos | Qdrant user_memory            │
│  持久化: SQLite (chat_history/global_agent_memory.sqlite) │
└──────────────────────────┬──────────────────────────────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                 ▼
┌─────────────┐  ┌─────────────┐  ┌──────────────────┐
│ vLLM 文本    │  │ vLLM 视觉    │  │ Qdrant 向量数据库 │
│ :8000        │  │ :8001        │  │ :6333             │
│ Qwen2.5-7B   │  │ Qwen2-VL-2B  │  │ 双路命名向量       │
│ AWQ 量化     │  │ AWQ 量化     │  │ vrag_hybrid       │
│ Prefix Cache │  │ Prefix Cache │  │ + user_memory     │
└─────────────┘  └─────────────┘  └──────────────────┘
```

### 2.2 技术栈一览

| 层级 | 技术选型 | 作用 |
|------|---------|------|
| **Agent 框架** | LangGraph (StateGraph) | 状态机驱动的多轮对话 + 工具调用路由 |
| **对话 LLM** | Qwen2.5-7B-Instruct-AWQ (vLLM) | 理解用户意图、决定调用哪个工具、最终答案合成 |
| **视觉语言模型** | Qwen2-VL-2B-Instruct-AWQ (vLLM) | 直接"看"论文页面图片并回答细节问题 |
| **视觉编码** | ColPali v1.2 (PaliGemma 架构, 8-bit 量化) | 将 PDF 页面渲染为图片后编码为 128 维多向量 |
| **文本编码** | BGE-M3 (Sentence-Transformers) | 中文友好的 1024 维文本稠密向量 |
| **向量数据库** | Qdrant (Docker) | 双路命名向量存储，支持 MAX_SIM 比较器 |
| **PDF 解析** | PyMuPDF (fitz) + pdf2image + pikepdf | 文本提取 + 表格检测 + 页面渲染 + 恶意代码扫描 |
| **学术检索** | Arxiv API + Semantic Scholar API | 全网论文检索 |
| **Web UI** | Chainlit | 异步消息通信，后台线程执行 Agent 避免 WebSocket 超时 |
| **对话持久化** | SQLite (LangGraph SqliteSaver) | 按 thread_id 隔离的多用户对话记忆 |
| **长期记忆** | Qdrant `user_memory` (BGE-M3 1024d) | 外部化记忆事实存储，每次提问时检索 Top-3 |
| **Prefix Caching** | vLLM `--enable-prefix-caching` | 缓存公共前缀 KV 矩阵，配合静态→动态 Prompt 排序确保高命中率 |

---

## 三、核心设计思路与创新点

### 3.1 视觉+文本双路 RAG（最核心创新）

**为什么需要双路？**

传统文本 RAG 对 CV 论文存在严重信息丢失：一篇目标检测论文的核心贡献往往在架构图上（FPN、PANet、Detection Head 结构），纯文本无法捕捉。ColPali 直接将整页论文渲染为图片，学习页面视觉布局与文字内容的联合表征，实现真正的"以图搜图"式检索。

**双路如何协作？**

```
用户 Query: "YOLOv10 的 neck 结构"
        │
        ├──→ [视觉路] ColPali 编码 query → Qdrant colpali_vision (128d, MAX_SIM)
        │       └──→ 返回 top_k*6 候选 (对架构图、表格页召回率高)
        │
        ├──→ [文本路] BGE-M3 编码 query → Qdrant text_dense (1024d, COSINE)
        │       └──→ 返回 top_k*6 候选 (对纯文本描述页召回率高)
        │
        └──→ RRF 倒数排名融合 (k=60) → 综合排序 → 截断 top_k
                │
                └──→ 文本摘要压缩 → VLM 多模态分析 → 最终答案
```

**RRF (Reciprocal Rank Fusion) 融合算法**：

```python
# 对每条命中的记录，累加其在两路中的倒数排名得分
score = 1/(k + rank_vision + 1) + 1/(k + rank_text + 1)
# k=60 对排名变化不敏感，平滑两路差异
# 在某一路排名高、另一路排名低的文档仍有机会胜出
```

### 3.2 Token 压缩流水线（解决 VLM 上下文爆炸）

VLM 直接处理多页论文图片 → 每张图约 2000 vision tokens，3 页就是 6000+ tokens。在送入 VLM 之前：

1. **文本摘要压缩**：本地 LLM (Qwen2.5-7B) 先将多页文本压缩为 ~500 字的中文摘要
2. **节省 90% Token**：原本 3 页 × 3000 字 = 9000 字 → 压缩为 500 字摘要
3. **表格还原**：PyMuPDF 检测到的表格转为 Markdown，替换页面文本中的占位符
4. **图片仅在必要时编码**：只对 RRF 融合后的 top_k 页面进行 Base64 图片编码

### 3.3 四层分层记忆系统（替换旧版摘要式压缩）

**设计动机**：旧版 `summary` 字符串在多次压缩后越来越模糊，无法区分核心指令、用户偏好、工作上下文和临时工具结果的不同生命周期。

**四层架构**：

```
Layer 1: 核心指令层 (System / SOP) — 永久保留，不可篡改
  内容: 系统 Prompt、Agent 行为规范、输出格式要求
  处理: 提取为模块级 CORE_INSTRUCTIONS 常量，配合 vLLM Prefix Cache 长久缓存

Layer 2: 实体与偏好层 (Entity & Profile) — 动态更新，Key-Value 存储
  内容: 研究兴趣（目标检测/域自适应/多模态等）、关注的论文 ID、偏好
  处理: 以 JSON 维护在 state.profile 中，regex + LLM 双重探测变化时更新

Layer 3: 工作上下文层 (Working Context) — 滑动窗口，保留近期逻辑
  内容: 最近 5 轮纯自然语言对话（用户问题 + AI 精炼回答）
  处理: 保持原样不压缩，超过窗口后旧对话移出（state.working_context）

Layer 4: 临时执行层 (Ephemeral Traces) — 替换为轻量备忘，非物理删除
  内容: 工具调用中间结果（RAG 截断文本、搜索列表、tool_calls 消息）
  处理: search_vision_knowledge 等工具返回的数千字结果替换为 ~100 字内部备忘，
        保留论文 ID、页码等溯源关键信息，后续追问时可重新调用工具获取详情
```

**State 定义**：

```python
class AgentState(TypedDict):
    messages: Annotated[list[BaseMessage], add_messages]
    profile: dict              # Layer 2: {research_interests, mentioned_papers}
    working_context: list      # Layer 3: [{question, answer, timestamp}, ...]
    core_instructions: str     # Layer 1: immutable SOP
```

**新图拓扑**：

```
START → init_core → retrieve_memory → token_check → agent ⇄ tools
                                                     ↓ (最终回答)
                                                  cleanup_ephemeral
                                                     ↓
                                                  update_profile → extract_memory (后台线程) → END
                                  
token_check → manage_context (token > 5000) → agent
```

### 3.4 外部化长期记忆 (Qdrant `user_memory`)

借鉴 Mem0 思想，将用户偏好/关注论文等"核心信息"外部化存储：

1. **提取**：`extract_memory_facts` 节点在后台线程中调用 LLM 从对话中提取记忆事实（格式：`「论文关注:xxx」`或`「用户偏好:xxx」`）
2. **编码**：BGE-M3 将每条事实编码为 1024 维向量
3. **存储**：upsert 到 Qdrant 新集合 `user_memory`（单路 dense 向量）
4. **检索**：`retrieve_long_term_memory` 节点在每次用户提问时，用 query 编码 → Qdrant 搜索 Top-3 → 注入到 Prompt 动态尾部
5. **非阻塞**：记忆提取全程在 daemon 线程中执行，不影响主流程响应速度

### 3.5 vLLM Prefix Caching + 缓存友好 Prompt 排序

**问题**：动态 profile/working_context/Qdrant 记忆事实如果放在 Prompt 头部，每次请求 Token 序列都不同，Prefix Cache 命中率为 0。

**解决方案 — 严格静态→动态倒序拼接**：

```
[头部 - 绝对静态] core_instructions (永不变 → vLLM 长久缓存)
    ↓
[中部 - 半静态] working_context (滑动窗口，仅尾部变动 → 前半部分命中缓存)
    ↓
[尾部 - 极度动态] profile + Qdrant 记忆事实 (紧贴用户 query)
    ↓
[用户消息] HumanMessage (本轮新问题)
```

配合 vLLM `--enable-prefix-caching` 启动参数，确保每次请求 ≥60-80% 的 Token 前缀命中显存缓存，显著降低 Time-To-First-Token。

### 3.6 ColPali ROI 视觉 Token 剪枝

**问题**：全页论文图片送入 VLM，每张高分辨率图动辄数千 vision tokens，其中大量无效背景（白边、无关段落）浪费显存和推理时间。

**解决方案 — 利用 ColPali Patch 级相似度热力图进行物理级"去噪过滤"**：

1. **热力图计算**：在 `search_vision_knowledge` 中，对每个检索到的页面，复用 ColPali query embedding（GPU 端），调用 `get_similarity_maps_from_embeddings()` 逐页计算 32×32 patch 级相似度热力图
2. **峰值定位**：在热力图上找 Top-5 峰值 patch，定位查询最相关的局部区域
3. **上下文扩边**：每个 ROI 向外扩展 25%（保留公式符号定义、表格列标题等周边上下文）
4. **坐标映射**：32×32 网格 → 原图分辨率（200 DPI 渲染）的线性缩放
5. **拼图合成**：若 ROI 数量超出 VLM 限制（`--limit-mm-per-prompt '{"image": 3}'`），用 Pillow 将所有 ROI 拼接为一张合成图
6. **回退机制**：若热力图标准差 < 0.3×均值（无显著热点），自动回退至全图模式

**效果**：节省约 60% 视觉 Token，同时保留核心图表/公式/表格的完整上下文。

### 3.4 GPU 显存精细化管理（单卡 24GB 的生存策略）

| 模型 | 显存分配 | 策略 |
|------|---------|------|
| ColPali v1.2 | ~3-4 GB | 8-bit 量化 + 懒加载（仅工具调用时初始化） |
| vLLM 文本 (Qwen2.5-7B) | ~8 GB | `--gpu-memory-utilization 0.35` |
| vLLM 视觉 (Qwen2-VL-2B) | ~6 GB | `--gpu-memory-utilization 0.30` + `--enforce-eager` |
| BGE-M3 | 0 GB (CPU) | 强制 CPU 推理，单页编码仅需几十毫秒 |

总计约 17-18 GB，在 24GB 显卡上留有余量。

### 3.5 PDF 安全防线

在下载和入库之间插入三层安全检查：

1. **文件大小检查**：拒绝 > 50MB 的异常文件
2. **pikepdf 结构扫描**：检测全局 JavaScript 注入 (`/Names/JavaScript`)、恶意打开动作 (`/OpenAction`)、页面级触发器 (`/AA`)
3. **可疑动作类型黑名单**：`/JavaScript`, `/Launch`, `/SubmitForm`, `/ImportData`

---

## 四、关键技术实现细节

### 4.1 Qdrant 双路命名向量 Schema

```python
Collection: vrag_hybrid_collection
├── colpali_vision: VectorParams(size=128, distance=COSINE,
│       multivector_config=MultiVectorConfig(comparator=MAX_SIM))
│   → ColPali 生成的 128 维多向量，MAX_SIM 比较器实现 ColBERT 风格的
│      late interaction（查询与文档的 token 级最大相似度求和）
│
└── text_dense: VectorParams(size=1024, distance=COSINE)
    → BGE-M3 生成的 1024 维稠密向量，标准余弦相似度检索

Payload:
  - paper_id: 论文标识符 (Arxiv ID)
  - file_name: 原始 PDF 文件名
  - page_number: 页码
  - image_path: 页面渲染 JPG 缓存路径
  - page_text: PyMuPDF 提取的清洗后文本
  - page_tables: 表格 Markdown 字典 {TABLE_0: "|col1|col2|..."}
```

### 4.2 ColPali 的 Late Interaction 机制

ColPali 基于 PaliGemma 架构，利用 ColBERT 风格的 late interaction：

- 不将页面压缩为单个向量，而是**保留每个 patch 的 token 级向量**（128 维 × N 个 tokens）
- 检索时 query 同样编码为多向量，计算 query 每个 token 与文档每个 token 的最大相似度之和（MAX_SIM）
- 优势：保留细粒度空间信息，对"图表 + 文字"混合布局的检索精度远超单向量方案

### 4.3 Monkey Patch 的原因与风险

```python
# 问题根源：
# transformers 的 _convert_peft_config_moe 尝试对 PEFT 配置做 str() 转换
# 但 ColPali 底层的 PaliGemma 架构不在其 MoE 白名单中 → KeyError: 'llava'
#
# 修复：
transformers.integrations.peft._convert_peft_config_moe = \
    lambda peft_config, model_type: peft_config
# 直接透传原始配置，跳过错误的字符串转换
```

### 4.4 表格提取的"空间碰撞检测"算法

PyMuPDF 的 `find_tables()` 返回表格坐标，`get_text("blocks")` 返回文本块坐标。通过检测两者的空间重叠关系（`Rect.intersects`），实现：

- 表格区域的原始文本被过滤（避免乱码）
- 在对应位置插入 `[TABLE_0_PLACEHOLDER]` 占位符
- 最终用 pandas DataFrame → Markdown 表格替换占位符
- 保留文档的阅读顺序（按 Y 坐标排序文本块）

---

## 五、Agent 工具集的 SOP（标准操作流程）

系统提示词为 LLM 定义了严格的工作流：

```
1. 用户询问论文细节 → 优先调用 search_vision_knowledge（查本地 RAG）
2. 本地无结果 → 调用 search_academic_papers（全英文关键词）
3. 搜索到新论文但未入库 → 强制执行"两步走"：
   Step A: download_and_ingest_vision_paper（下载 + 双路入库）
   Step B: search_vision_knowledge（自动检索刚入库的论文）
4. 最终回答 → Markdown 格式 + 必须附带引用来源
```

### 三个工具的职责边界

| 工具 | 触发条件 | 输入 | 输出 |
|------|---------|------|------|
| `search_academic_papers` | 本地无相关论文，需全网搜索 | 两个英文关键词 + max_results | Arxiv/Semantic Scholar 检索结果列表 |
| `download_and_ingest_vision_paper` | 搜索到新论文但未入库 | paper_id + pdf_url | 双路向量入库确认 |
| `search_vision_knowledge` | 询问已入库论文的内部细节 | 英文 query + 可选 paper_id | VLM 多模态图文分析报告 |

---

## 六、性能与评估体系

### 6.1 效能遥测（Telemetry）

`agent_core.py` 的 `call_model` 节点拦截每次 LLM 调用，记录：

- **E2E 延迟**：从请求发出到完整响应的时间
- **输入/输出 Token 数**：从 vLLM 的 `usage_metadata` 提取
- **TPOT (Time Per Output Token)**：每个生成 token 的平均耗时
- **Throughput**：每秒生成的 token 数

### 6.2 RAG 评估框架 (`eval_system.py`)

三个评估维度：

1. **检索召回率 (Recall)**：命中的正确页码 / 预设总正确页码
2. **检索精确度 (Precision)**：命中的正确页码 / 实际检索返回的页码
3. **生成忠实度 (Faithfulness)**：LLM-as-Judge，判断回答中的事实是否都能在检索上下文中找到依据

### 6.3 关键性能参数

| 参数 | 值 | 说明 |
|------|-----|------|
| RRF k 值 | 60 | 对排名变化不敏感，适合两路差异大的场景 |
| 候选池大小 | top_k × 6 | 扩大候选池给 RRF 更多融合空间 |
| Token 压缩阈值 | 5000 | 超过后触发工作上下文管理 |
| 工作上下文保留轮数 | 5 | Layer 3 滑动窗口大小 |
| 长期记忆检索 Top-K | 3 | 每次提问从 Qdrant user_memory 检索的事实数 |
| ROI 上下文边距 | 25% | 裁剪区域向外扩展比例 |
| ROI 峰值数量 | Top-5 | 每页最多提取的 ROI 区域数 |
| 热力图回退阈值 | std < 0.3×mean | 均匀分布时自动回退全图 |
| 文本摘要上限 | ~500 字 | 本地 LLM 压缩多页文本 |
| VLM 最大图片数 | 3 张/请求 | `--limit-mm-per-prompt`，超出自动拼图 |
| 单次 vLLM 最大 token | 8192 (文本) / 12288 (视觉) | `--max-model-len` 配置 |
| Prefix Cache 命中率 | ≥60% | 配合 Prompt 静态→动态倒序拼接 |

---

## 七、项目文件职责速查

| 文件 | 职责 | 关键函数/类 |
|------|------|------------|
| `config.py` | 全局配置中心（路径、模型名、超参数、记忆系统参数） | `Config` 类 |
| `agent_core.py` | LangGraph 状态机 + 四层分层记忆 + Prefix Cache 友好 Prompt + 遥测 + CLI | `call_model`, `init_core_instructions`, `cleanup_ephemeral`, `manage_working_context`, `update_profile`, `extract_memory_facts`, `retrieve_long_term_memory` |
| `app_ui.py` | Chainlit Web UI，后台线程执行 Agent，即时回答流式展示 | `on_chat_start`, `on_message` |
| `tools/vision_rag.py` | 三个 Agent 工具 + 模型懒加载 + RRF 融合 + **ROI 智能裁剪** + VLM 分析 | `search_academic_papers`, `download_and_ingest_vision_paper`, `search_vision_knowledge`, `_compute_similarity_heatmap`, `_heatmap_to_roi`, `_stitch_rois_to_composite` |
| `tools/mix_ingest.py` | 批量双路 PDF 入库 + 文本/表格提取 | `ingest_papers_to_qdrant_hybrid`, `extract_text_and_tables_from_page` |
| `tools/vision_ingest.py` | 纯视觉入库（仅 ColPali，无文本路） | — |
| `tools/text_ingest.py` | 纯文本入库（LlamaIndex 管线，遗留方案） | — |
| `eval_system.py` | RAG 评估框架（检索+生成质量） | `VRAGEvaluator` |
| `download_models.py` | 从 ModelScope 下载 BGE-M3 | — |
| `download_llm.py` | 从 ModelScope 下载 Qwen2-VL | — |

---

## 八、启动依赖链

```
Docker (Qdrant :6333)  ──→  vLLM Text (:8000)  ──→  vLLM Vision (:8001)  ──→  Chainlit (:8053)
                                                                                   │
                                                                                   └── 或 CLI: python agent_core.py
```

四个服务**全部启动**后系统才具有完整功能。缺少 vLLM Vision 时仍可检索但无法分析图片内容；缺少 Qdrant 时无法使用本地知识库。
